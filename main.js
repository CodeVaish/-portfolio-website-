var type = new Typed(".text", {
    strings: ["Frontend Developer", "Backend Developer", "Web Developer"], // Corrected strings array
    typeSpeed: 100,   // Missing comma fixed
    backSpeed: 100,   // Missing comma fixed
    backDelay: 100,  // Missing comma fixed
    loop: true        // Proper syntax
});
